@model IEnumerable<Transportation.Application.ViewModels.ClasseVagaoViewModel>

@{
    ViewBag.Title = "Cadastro de Classes de Vagões";
}

<h2>Classes de Vagões</h2>

<p>
    @Html.ActionLink("Novo", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Codigo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Descricao)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Codigo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Descricao)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ClasseVagaoId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ClasseVagaoId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ClasseVagaoId })
        </td>
    </tr>
}

</table>
